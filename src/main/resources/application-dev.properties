server.address=0.0.0.0
server.port=9999



spring.datasource.url=jdbc:postgresql://localhost:5432/e_commerce_spring
spring.datasource.username=postgres
spring.datasource.password=123

# ==============================
# Database Configuration for production
# ==============================

# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}



spring.datasource.driver-class-name=org.postgresql.Driver



# Hibernate properties (Optional, but recommended for JPA configuration)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Disable SQL logging
logging.level.org.hibernate.SQL=ERROR

# Disable binding parameter logs
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.enabled=true

file.upload-dir=/app/uploads/

server.servlet.session.timeout=30m
#mail config
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=25
spring.mail.username=3970652266de4f
spring.mail.password=009f3527de8a53
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==============================
# Mail Configuration for production
# ==============================

# spring.mail.host=${SPRING_MAIL_HOST}
# spring.mail.port=${SPRING_MAIL_PORT}
# spring.mail.username=${SPRING_MAIL_USERNAME}
# spring.mail.password=${SPRING_MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH}
# spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE}

# Image processing configuration
app.image.default-avatar-path=static/images/avatar.png
app.image.max-width=800
app.image.max-height=800
app.image.compression-quality=0.7
app.image.max-file-size=1048576


server.error.include-message=always
server.error.include-binding-errors=always
server.error.whitelabel.enabled=false
# Elasticsearch URLn

#spring.data.elasticsearch.cluster-name=productService
#spring.data.elasticsearch.cluster-nodes=http://localhost:9200
#spring.elasticsearch.uris=http://localhost:9200

# Optional: Uncomment if Elasticsearch requires authentication
# spring.elasticsearch.username=your_username
# spring.elasticsearch.password=your_password

# Optional: Connection timeout settings
#spring.elasticsearch.connection-timeout=3s
#spring.elasticsearch.socket-timeout=3s

spring.main.allow-bean-definition-overriding=true


